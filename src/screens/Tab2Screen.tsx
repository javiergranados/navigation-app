import React from 'react';
import { appStyles } from '../theme/appTheme';
import { Text, View } from 'react-native';

const Tab2Screen = () => {
  return (
    <View>
      <Text style={appStyles.title}>Tab2 screen</Text>
    </View>
  );
};

export default Tab2Screen;
