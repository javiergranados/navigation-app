import React from 'react';
import { Text, View } from 'react-native';
import { appStyles } from '../theme/appTheme';

const ChatScreen = () => {
  return (
    <View>
      <Text style={appStyles.title}>Chat screen</Text>
    </View>
  );
};

export default ChatScreen;
